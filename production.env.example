# production.env.example
# Production environment configuration
# Copy to production server and fill with real values
# SECURITY: Never commit actual secrets, use environment variables or secrets manager

# ===== Environment =====
NODE_ENV=production
PORT=3000

# ===== Application =====
APP_BASE_URL=https://app.seuprojeto.com
APP_NAME=Páginas do Comércio

# ===== Database (PostgreSQL) =====
# Use managed database service (AWS RDS, Heroku, Railway, etc.)
# Connection string with all credentials
DATABASE_URL=postgresql://app:CHANGE_ME_STRONG_PASSWORD@db.seuprojeto.com:5432/paginas_comercio_prod?schema=public&sslmode=require

# ===== Redis Cache =====
# Use managed Redis service (AWS ElastiCache, Heroku Redis, etc.)
REDIS_URL=redis://CHANGE_ME:CHANGE_ME_STRONG_PASSWORD@redis.seuprojeto.com:6379

# ===== Authentication =====
NEXTAUTH_URL=https://app.seuprojeto.com
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=CHANGE_ME_VERY_LONG_RANDOM_STRING

# ===== JWT =====
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_VERY_LONG_RANDOM_STRING
JWT_EXPIRATION=24h

# ===== Cloudflare R2 (Static Deploy) =====
# https://dash.cloudflare.com/profile/api-tokens
R2_ACCOUNT_ID=CHANGE_ME
R2_ACCESS_KEY_ID=CHANGE_ME
R2_SECRET_ACCESS_KEY=CHANGE_ME
R2_BUCKET_NAME=paginas-comercio-prod
R2_PUBLIC_BASE_URL=https://static.seuprojeto.com

# ===== SEO Configuration =====
SEO_DEFAULT_LOCALE=pt-BR
SEO_SUPPORTED_LOCALES=pt-BR,en-US,es-ES

# ===== Logging & Observability =====
LOG_LEVEL=info
# Sentry DSN for error tracking: https://sentry.io
SENTRY_DSN=https://CHANGE_ME@sentry.io/CHANGE_ME

# ===== Email (SendGrid, AWS SES, etc.) =====
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=CHANGE_ME_SENDGRID_API_KEY
SMTP_FROM=noreply@seuprojeto.com

# ===== External APIs =====
# Google Search Console API Key
GOOGLE_SEARCH_CONSOLE_KEY=CHANGE_ME

# ===== Monitoring =====
# DataDog, New Relic, etc.
MONITORING_API_KEY=CHANGE_ME

# ===== Feature Flags =====
FEATURE_ANALYTICS=true
FEATURE_NOTIFICATIONS=true
FEATURE_EXPORT_STATIC=true

# ===== SSL/TLS =====
# If using self-signed or custom certificates
# NODE_TLS_REJECT_UNAUTHORIZED=0 # ONLY FOR DEVELOPMENT

# ===== Rate Limiting =====
RATE_LIMIT_PING_PER_HOUR=10
RATE_LIMIT_REGENERATE_PER_HOUR=5

# ===== Security Headers =====
CORS_ALLOWED_ORIGINS=https://app.seuprojeto.com,https://admin.seuprojeto.com

# ===== Database Backups =====
# Scheduled backups
BACKUP_ENABLED=true
BACKUP_FREQUENCY=daily
BACKUP_TIME=02:00 UTC

# ===== Disaster Recovery =====
# Keep at least 30 days of backups
BACKUP_RETENTION_DAYS=30
